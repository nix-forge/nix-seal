# shellcheck shell=bash
# Intentionally inert. A durable direnv approval must never authorize mutable
# worktree Nix expressions, shell hooks, or repository executables after a
# branch checkout. Enter the reviewed development environment explicitly with
# `nix develop`.
